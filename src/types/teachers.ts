export interface Teacher {
  id: string;
  functionalCode: string;
  firstName: string;
  lastName: string;
  name: string; // Combined
  birthDate: string;
  rank: string;
  subject: string;
  grade: string;
  effectiveDate: string;
  email: string;
  levels: string[];
}
